# Copy this file to .env.local and fill in values when you're ready to
# wire up the pieces that are currently deferred (see STACK.md).

# --- Contact form email delivery (NOT wired up yet) ---------------------
# Once you install `resend` and update src/app/api/contact/route.ts:
# RESEND_API_KEY=
# CONTACT_TO_EMAIL=marci@example.com

# --- Real MLS/IDX feed (NOT wired up yet) --------------------------------
# Once you have a vendor account (IDX Broker, Spark API, Bridge
# Interactive, etc.) and update src/lib/listings.ts:
# MLS_API_KEY=
# MLS_API_BASE_URL=
